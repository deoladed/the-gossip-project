<center><br><h1>WELCOME <%= User.all.last.first_name.upcase %></h1><br>
<p>Hey salut <%= User.all.last.first_name %> ! Bienvenue sur THE site de potins. <span class='font-weight-bold'>Eclate toi et viens cracher sur tes copains !</span></p><br></center>

<div>
	<% @potins.each do |potin| %>
<div class="card text-center">
  <div class="card-header">
    <%= potin.user.first_name %> a dit :
  </div>
  <div class="card-body">
    <h5 class="card-title"><%= potin.title %></h5>
    <p class="card-text"></p>
    <%= link_to "Voir le potin", potins_path(potin.id), class:"btn btn-primary" %>
  </div>
  <div class="card-footer text-muted">
    <%= potin.date.to_s[0..-13] %>
  </div>
</div>
<br>
	<% end %>         
</div>
