<h1>Welcome <%= params[:name] %></h1>
<p>Hey salut <%= params[:name] %> ! Bienvenue sur THE site de potins. <span class='font-weight-bold'>Eclate toi et viens cracher sur tes copains !</span></p>

<div>
	<% @potins.each do |potin| %>
	<div class="alert alert-dark alert-dismissible fade show" role="alert">
		<%= link_to potin.title, potins_path(potin.id), class:"alert-link" %>
			<button type="button" class="close" data-dismiss="alert" aria-label="Close">
			<span aria-hidden="true">&times;</span>
		</button>
		<hr>
		par <%= potin.user.first_name %>
	</div>
	<% end %>         
</div>